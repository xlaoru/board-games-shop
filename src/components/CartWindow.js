import React from 'react';
import CartList from '../containers/CartList'

const CartWindow = () => {
    return (
        <div>
            <CartList/>
        </div>
    );
};

export default CartWindow;